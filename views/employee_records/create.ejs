<%- include('../partials/navbar') %>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
<link rel="stylesheet" href="/css/style.css">

<main class="container my-5">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
      <div class="card shadow-sm">
        <div class="card-body">
          <h1 class="text-primary mb-4">Create New Employee</h1>
          
          <% if (typeof error !== 'undefined') { %>
            <div class="alert alert-danger">
              <%= error %>
            </div>
          <% } %>

          <form action="/create" method="POST">
            <div class="mb-3">
              <label for="name" class="form-label">Full Name:</label>
              <input type="text" class="form-control" id="name" name="name" value="<%= employee.name || '' %>" required>
            </div>

            <div class="mb-3">
              <label for="position" class="form-label">Position:</label>
              <input type="text" class="form-control" id="position" name="position" value="<%= employee.position || '' %>" required>
            </div>

            <div class="mb-3">
              <label for="department" class="form-label">Department:</label>
              <input type="text" class="form-control" id="department" name="department" value="<%= employee.department || '' %>" required>
            </div>

            <div class="mb-3">
              <label for="contact_info" class="form-label">Contact Information:</label>
              <input type="text" class="form-control" id="contact_info" name="contact_info" value="<%= employee.contact_info || '' %>" required>
            </div>

            <div class="mb-3">
              <label for="employment_status" class="form-label">Employment Status:</label>
              <select class="form-select" id="employment_status" name="employment_status" required>
                <option value="">Select Status</option>
                <option value="Full-time" <%= employee.employment_status === 'Full-time' ? 'selected' : '' %>>Full-time</option>
                <option value="Part-time" <%= employee.employment_status === 'Part-time' ? 'selected' : '' %>>Part-time</option>
                <option value="Contract" <%= employee.employment_status === 'Contract' ? 'selected' : '' %>>Contract</option>
                <option value="Intern" <%= employee.employment_status === 'Intern' ? 'selected' : '' %>>Intern</option>
              </select>
            </div>

            <div class="d-flex gap-2">
              <button type="submit" class="btn btn-success">Create Employee</button>
              <a href="/employee_records" class="btn btn-secondary">Cancel</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</main>